import basis from '../../../constant/basis'
const state = {
  tableData: []                             // 原始记录表数据
}

const actions = {
  // changeCapacity(context, { capacity }) {
  //   context.commit('MEASURE_CHANGE_CAPACITY', { name })
  // }
}

const mutations = {
  // APP_CHANGE_ROUTER(state, { name, params }) {
  //   console.log('===========')
  //   state.navActive = '/' + name
  // }
}

export default {
  state,
  mutations,
  actions
}